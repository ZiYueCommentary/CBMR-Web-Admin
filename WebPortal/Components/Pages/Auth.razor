@page "/auth"

@using Microsoft.AspNetCore.Authorization

@attribute [Authorize]

<PageTitle>@Localizer["Auth"]</PageTitle>

<h1>@Localizer["Authenticated"]</h1>

<AuthorizeView>
    @Localizer["HelloUser", context.User.Identity?.Name]
</AuthorizeView>